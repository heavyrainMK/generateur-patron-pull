from swatch import Swatch

class Sleeve:

    def __init__(self, swatch, desired_width, desired_length)
        self.swatch = swatch
        self.desired_width = desired_width
        self.desired_length = desired_length
        current_width = 0
        current_length = 0
        desired_stitches = 0
        current_stitches = 0
        desired_rows = 0
        current_rows = 0

